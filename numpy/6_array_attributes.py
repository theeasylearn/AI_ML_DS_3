import numpy as np 
arr = np.array([[1, 2, 3], [4, 5, 6]])
#get size of 
print("Shape(how many rows and columns) of array =",arr.shape)
print("No of elements (row * column) array =",arr.size)
print("spaced occupied by array =",arr.itemsize)
print("Dimensions of array =",arr.ndim)
print("Transpose of array ",arr.T)